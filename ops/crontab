# Backup runs before retention, so each day's dump still has the rows that
# are about to be deleted. Times are container-local (UTC unless TZ is set).
0 2 * * * . /app/env.sh && python3 /app/backup.py >> /proc/1/fd/1 2>&1
30 2 * * * . /app/env.sh && python3 /app/retention.py >> /proc/1/fd/1 2>&1
